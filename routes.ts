export const publicRoutes = ["/", "/products", "/auth/sign-in", "/auth/sign-up", "/auth/new-verification", "/auth/reset", "/auth/new-password"];

export const authRoutes = ["/favourites", "/orders", "/settings"];

export const apiAuthRoutesPrefix = "/api/auth";

export const DEFAULT_REDIRECT_LOGIN = "/";
